# Base URL of the backend REST API (include /api/v1).
VITE_API_URL=https://your-backend.onrender.com/api/v1

# Origin of the backend for the Socket.IO connection (NO /api/v1).
VITE_SOCKET_URL=https://your-backend.onrender.com
